import { Navbar } from 'components/shared/navbar/Navbar';
import './globals.css';

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

interface BodyProps {
  children: React.ReactNode;
  bodyBgColor?: string;
}

export default function RootLayout({ children, bodyBgColor }: BodyProps) {
  console.log(bodyBgColor);
  return (
    <html lang="en">
      <body className="flex flex-col items-center justify-between w-screen h-screen bg-teal">
        {children}
      </body>
    </html>
  );
}
